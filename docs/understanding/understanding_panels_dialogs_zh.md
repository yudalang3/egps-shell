# 共享面板与对话框理解
本节详细列出该类别的类、所在路径和职责描述。

- **AllocatorPanel**（`src/egps2/panels/dialog/AllocatorPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **CallBackTransferBehevior**（`src/egps2/panels/dialog/opendialog/CallBackTransferBehevior.java`）：供模块复用的 Swing 面板或对话框组件。
- **ColorCapture**（`src/egps2/panels/dialog/ColorCapture.java`）：供模块复用的 Swing 面板或对话框组件。
- **CustomizeFontEnum**（`src/egps2/panels/graphicpro/CustomizeFontEnum.java`）：供模块复用的 Swing 面板或对话框组件。
- **DialogUtil**（`src/egps2/panels/DialogUtil.java`）：供模块复用的 Swing 面板或对话框组件。
- **DynGifLabel**（`src/egps2/panels/DynGifLabel.java`）：供模块复用的 Swing 面板或对话框组件。
- **EGPSColorChooser**（`src/egps2/panels/dialog/EGPSColorChooser.java`）：供模块复用的 Swing 面板或对话框组件。
- **EGPSFileChooser**（`src/egps2/panels/dialog/EGPSFileChooser.java`）：供模块复用的 Swing 面板或对话框组件。
- **EGPSFontChooser**（`src/egps2/panels/dialog/EGPSFontChooser.java`）：供模块复用的 Swing 面板或对话框组件。
- **EGPSJSpinner**（`src/egps2/panels/dialog/EGPSJSpinner.java`）：供模块复用的 Swing 面板或对话框组件。
- **ExternalProgramSettingJPanel**（`src/egps2/panels/pref/ExternalProgramSettingJPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **FontAppearancePanel**（`src/egps2/panels/FontAppearancePanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **GradientPanel**（`src/egps2/panels/dialog/GradientPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **GraphicPropertiesPanel**（`src/egps2/panels/graphicpro/GraphicPropertiesPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **GUIPrintStream**（`src/egps2/panels/GUIPrintStream.java`）：供模块复用的 Swing 面板或对话框组件。
- **HistoryPanel**（`src/egps2/panels/HistoryPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **HtmlDocumentPanel**（`src/egps2/panels/pref/HtmlDocumentPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **IconSizePrefJPanel**（`src/egps2/panels/pref/IconSizePrefJPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **InformationPanelFactory**（`src/egps2/panels/InformationPanelFactory.java`）：供模块复用的 Swing 面板或对话框组件。
- **InstantStatusPanel**（`src/egps2/panels/reusablecom/InstantStatusPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **JIMSendTextPane**（`src/egps2/panels/JIMSendTextPane.java`）：供模块复用的 Swing 面板或对话框组件。
- **LanguageSettingPanel**（`src/egps2/panels/pref/LanguageSettingPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **LaunchProperty4ProgramicConfig**（`src/egps2/panels/pref/LaunchProperty4ProgramicConfig.java`）：供模块复用的 Swing 面板或对话框组件。
- **LicenseOfEGPSPanel**（`src/egps2/panels/LicenseOfEGPSPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **LookAndFeelPanel**（`src/egps2/panels/pref/LookAndFeelPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **MailUtil**（`src/egps2/panels/bugreporter/MailUtil.java`）：供模块复用的 Swing 面板或对话框组件。
- **ModuleActionsPanel**（`src/egps2/panels/pref/ModuleActionsPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **ModuleInspector**（`src/egps2/panels/ModuleInspector.java`）：供模块复用的 Swing 面板或对话框组件。
- **ParameterInitialized**（`src/egps2/panels/reusablecom/ParameterInitialized.java`）：供模块复用的 Swing 面板或对话框组件。
- **PureStringsDispalyDialog**（`src/egps2/panels/dialog/PureStringsDispalyDialog.java`）：供模块复用的 Swing 面板或对话框组件。
- **ReflectDialog**（`src/egps2/panels/ReflectDialog.java`）：供模块复用的 Swing 面板或对话框组件。
- **ReflectDialogContorller**（`src/egps2/panels/bugreporter/ReflectDialogContorller.java`）：供模块复用的 Swing 面板或对话框组件。
- **ReusableCompoentBuilder**（`src/egps2/panels/reusablecom/ReusableCompoentBuilder.java`）：供模块复用的 Swing 面板或对话框组件。
- **SendEmail_text**（`src/test/java/egps2/panels/bugreporter/SendEmail_text.java`）：供模块复用的 Swing 面板或对话框组件。
- **SendEmailAttachment**（`src/test/java/egps2/panels/bugreporter/SendEmailAttachment.java`）：供模块复用的 Swing 面板或对话框组件。
- **SendEmailSMTP**（`src/test/java/egps2/panels/bugreporter/SendEmailSMTP.java`）：供模块复用的 Swing 面板或对话框组件。
- **StaticsPanel**（`src/egps2/panels/StaticsPanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **StdErrConsolePanel**（`src/egps2/panels/StdErrConsolePanel.java`）：供模块复用的 Swing 面板或对话框组件。
- **StingArrayDisplayDialog**（`src/egps2/panels/dialog/opendialog/StingArrayDisplayDialog.java`）：供模块复用的 Swing 面板或对话框组件。
- **StringDisplayAndChangeDialog**（`src/egps2/panels/dialog/opendialog/StringDisplayAndChangeDialog.java`）：供模块复用的 Swing 面板或对话框组件。
- **StringSearchDialog**（`src/egps2/panels/dialog/opendialog/StringSearchDialog.java`）：供模块复用的 Swing 面板或对话框组件。
- **SubTextPanelNode**（`src/egps2/panels/SubTextPanelNode.java`）：供模块复用的 Swing 面板或对话框组件。
- **SwingDialog**（`src/egps2/panels/dialog/SwingDialog.java`）：供模块复用的 Swing 面板或对话框组件。
- **TextAntiAliasPanel**（`src/egps2/panels/pref/TextAntiAliasPanel.java`）：供模块复用的 Swing 面板或对话框组件。
